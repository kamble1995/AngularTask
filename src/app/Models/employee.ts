export interface IEmployee {
    id: string;
    name: string;
    email: string;
    department: string;
    joiningDate: string;
}

export interface IDepartments{
    name : string,
    value : string
}